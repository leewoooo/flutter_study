import 'package:flutter/material.dart';

class MemoUpdate extends StatelessWidget {
  const MemoUpdate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Update'),
      ),
    );
  }
}
